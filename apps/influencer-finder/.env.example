NIMBLE_API_KEY=your_nimble_api_key
# Supabase store (optional; app falls back to local cache when unset)
SUPABASE_URL=https://your-project.supabase.co
# Use the anon/publishable key, not the service_role key (service_role bypasses RLS)
SUPABASE_KEY=your_anon_key
IF_CONCURRENCY=3
IF_EFFORT=high
