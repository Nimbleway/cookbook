"""Offline grounding-verifier eval harness.

Run with: `python -m agent.eval` (offline by default; pass --live to also run any
cases flagged as needing live calls — there are none in the shipped suite).
"""
