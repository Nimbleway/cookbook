# secrets — never commit
.env
service-account.json
*.json.key

# account-specific (regenerated by setup_agent.py)
agents.json

# python
__pycache__/
*.pyc
.venv/
venv/

# regenerable local outputs (the built data/*.json demo cache IS committed)
data/mcm.db
data/enforcement_tracker.csv
data/smoke_*.json
notices/
